# example 1:

pet = ["cat", "dog", "fish", "rabbit", "horse", "bird"]
#Extracting a portion of the list from 2nd to 5th element 
print(pet[2:5])
print(pet[-3:-1])
#get all items after a given index 
print(pet[3:])

# example 2:

pet = ["cat", "dog", "fish", "rabbit", "horse", "bird"]
#slicing elements from index 1 to 4 at intervals of 2
print(pet[0:5:2])
